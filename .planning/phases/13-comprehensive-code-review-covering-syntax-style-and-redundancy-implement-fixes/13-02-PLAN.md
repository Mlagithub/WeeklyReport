---
phase: 13-comprehensive-code-review-covering-syntax-style-and-redundancy-implement-fixes
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - app.py
  - routes.py
  - utils.py
  - config.py
  - extensions.py
  - forms.py
  - models.py
  - gunicorn.conf.py
  - exporters/__init__.py
  - exporters/base.py
  - exporters/docx.py
  - exporters/excel.py
  - exporters/pdf.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "40 auto-fixable linting issues are resolved"
    - "All Python files have EOF newlines"
    - "Unused imports removed from app.py, utils.py, exporters/docx.py"
    - "ruff check shows reduced issue count (54 to ~14)"
  artifacts:
    - path: "app.py"
      provides: "Application entry point with clean imports"
      after_fix: "No F401 unused import errors"
    - path: "routes.py"
      provides: "Route handlers with clean code"
      after_fix: "No F541 f-string errors"
    - path: "utils.py"
      provides: "Utility functions with clean imports"
      after_fix: "No F401 or E303 errors"
    - path: "exporters/docx.py"
      provides: "DOCX exporter"
      after_fix: "No F401 unused import errors"
  key_links:
    - from: "ruff CLI"
      to: "all Python files"
      via: "ruff check . --fix"
      pattern: "ruff check.*--fix"
---

<objective>
Auto-fix 40 linting issues using ruff --fix, including unused imports, missing EOF newlines, blank line whitespace, continuation indentation, and f-string placeholders.

Purpose: Eliminate trivial style issues automatically before addressing complex manual fixes.
Output: 12 Python files with auto-fixable linting issues resolved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-comprehensive-code-review-covering-syntax-style-and-redundancy-implement-fixes/13-RESEARCH.md
@.planning/phases/13-comprehensive-code-review-covering-syntax-style-and-redundancy-implement-fixes/13-01-PLAN.md

<interfaces>
<!-- Auto-fixable issues per RESEARCH.md -->

By Category (40 auto-fixable):
- Unused imports (F401): 24 issues
- Missing EOF newline (W292): 11 issues
- Blank line whitespace (W293): 9 issues
- Too many blank lines (E303): 6 issues
- Continuation indent (E128): 12 issues
- F-string no placeholder (F541): 2 issues
- Unused variable (F841): 3 issues

Files affected:
- routes.py: E128 indentation fixes, F541 f-string fixes
- app.py: F401 unused imports (DevelopmentConfig, ProductionConfig, form classes)
- utils.py: F401 unused imports (relativedelta, Color), E303 blank lines
- exporters/__init__.py: W292 EOF newline
- exporters/docx.py: F401 unused imports (Pt, os, datetime, unquote)
- exporters/pdf.py: W292 EOF newline (W605 handled separately in plan 03)
- exporters/excel.py: W292 EOF newline
- config.py, extensions.py, forms.py, models.py, gunicorn.conf.py: W292 EOF newline
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run ruff auto-fix on all Python files</name>
  <files>
    app.py, routes.py, utils.py, config.py, extensions.py, forms.py, models.py,
    gunicorn.conf.py, exporters/__init__.py, exporters/base.py, exporters/docx.py,
    exporters/excel.py, exporters/pdf.py
  </files>
  <read_first>
    - app.py (to see current unused imports state)
    - routes.py (to see current F541 and E128 issues)
    - utils.py (to see current F401 and E303 issues)
    - exporters/docx.py (to see unused imports)
    - exporters/__init__.py (to see EOF newline state)
  </read_first>
  <behavior>
    - ruff check . --fix runs without errors
    - 40 auto-fixable issues are resolved
    - All Python files have EOF newlines
    - Unused imports removed from app.py, utils.py, exporters/docx.py
    - F-string without placeholder converted to regular string in routes.py
  </behavior>
  <action>
Execute ruff auto-fix command:

```bash
ruff check . --fix
```

This will automatically fix:
1. F401 (unused imports) - removes unreferenced import statements
2. W292 (missing EOF newline) - adds newline at end of files
3. W293 (blank line whitespace) - removes trailing whitespace on blank lines
4. E303 (too many blank lines) - reduces excessive blank lines
5. E128 (continuation indent) - fixes alignment in multi-line constructs
6. F541 (f-string no placeholder) - converts f"...text..." to "...text..."
7. F841 (unused variable) - removes unused local variables

After running, verify the fixes by checking:
- All files end with a newline (no W292 errors remain)
- routes.py line 285: flash(f'数据己删除') should become flash('数据己删除')
- Unused imports removed (check with grep)

Run pytest after fixes to ensure no behavior changes:
```bash
pytest tests/ -q
```
</action>
  <verify>
    <automated>pytest tests/ -q && ruff check . --statistics | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - ruff check . --fix completes without errors
    - ruff check . --statistics shows reduced issue count (from 54 to ~14 remaining)
    - All 12 files have EOF newline (check with `tail -c 1 filename | od -c` showing newline)
    - routes.py line 285 contains flash('数据己删除') (not f-string)
    - pytest tests/ -q passes (all existing tests work)
  </acceptance_criteria>
  <done>40 auto-fixable linting issues resolved across all Python files.</done>
</task>

<task type="auto">
  <name>Task 2: Verify auto-fix results and document remaining issues</name>
  <files>N/A</files>
  <read_first>
    - All modified files (to verify fix quality)
  </read_first>
  <behavior>
    - Remaining manual-fix issues documented (14 issues)
    - Complexity metrics unchanged (CC values for high-complexity functions)
  </behavior>
  <action>
Run diagnostic commands to verify auto-fix results and identify remaining manual issues:

1. Count remaining linting issues:
```bash
ruff check . --statistics
```

Expected remaining issues (14 manual-fix required):
- E402 (import not at top): 5 issues in app.py, utils.py, exporters/__init__.py
- F811/F402 (variable shadowing): 5 issues in routes.py
- W605 (invalid escape): 2 issues in exporters/pdf.py lines 232, 240

2. Check complexity metrics unchanged:
```bash
radon cc . -a -s
```

Expected high-complexity functions (unchanged by auto-fix):
- ExcelExporter._generate (CC=21)
- RecordDownloader.download (CC=12)
- build_record_query (CC=12)

3. Verify EOF newlines added:
```bash
for f in app.py routes.py utils.py config.py extensions.py forms.py models.py gunicorn.conf.py exporters/__init__.py exporters/base.py exporters/docx.py exporters/excel.py exporters/pdf.py; do
  if [ -f "$f" ]; then
    last_char=$(tail -c 1 "$f" | od -c | head -1)
    echo "$f: $last_char"
  fi
done
```

All should show '\n' (newline) as last character.
</action>
  <verify>
    <automated>ruff check . --statistics | head -20</automated>
  </verify>
  <acceptance_criteria>
    - ruff check shows ~14 remaining issues (E402, F811, W605 types)
    - radon cc output unchanged for high-complexity functions
    - All 12 files end with newline character
    - pytest tests/ -q passes
  </acceptance_criteria>
  <done>Auto-fix verification complete, 14 manual-fix issues identified for plan 03.</done>
</task>

</tasks>

<verification>
After plan completion:
1. ruff check . --statistics shows 54 total issues reduced to ~14 manual-fix issues
2. All modified files have EOF newlines
3. Unused imports removed from app.py, utils.py, exporters/docx.py
4. pytest tests/ -q passes
</verification>

<success_criteria>
- 40 auto-fixable issues resolved
- Remaining 14 manual-fix issues documented
- All existing tests pass
- EOF newlines added to all Python files
</success_criteria>

<output>
After completion, create `.planning/phases/13-comprehensive-code-review-covering-syntax-style-and-redundancy-implement-fixes/13-02-SUMMARY.md`
</output>