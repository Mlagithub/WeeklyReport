---
phase: 17-personal-summary-generation
plan: 03
type: execute
wave: 3
depends_on: [17-02]
files_modified: []
autonomous: false
requirements: [SUMMARY-01, SUMMARY-02, SUMMARY-03, SUMMARY-04, UI-01, UI-02]
user_setup: []

must_haves:
  truths:
    - "User can see AI Summary card on home page"
    - "User can select time range and generate summary"
    - "Loading indicator appears during generation"
    - "Generated summary displays correctly"
    - "Copy button works"
    - "Error messages display in Chinese"
  artifacts:
    - path: "templates/home.html"
      provides: "Functional AI Summary UI"
      test_url: "/"
  key_links: []
---

<objective>
Verify complete personal summary generation feature through user testing.

Purpose: Confirm all UI interactions work correctly and AI integration produces expected results.
Output: User-verified feature ready for milestone completion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- All prior phase and plan summaries loaded by executor -->
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete personal summary generation feature:

    1. **AI Summary card** on home page with:
       - Time range dropdown (本周/本月/本季度/本年)
       - Template dropdown (populated from AITemplate table)
       - Custom prompt input field
       - Generate button

    2. **Backend integration**:
       - /generate-summary POST route returning JSON
       - summary_utils.py with fetch_user_records, assemble_prompt, generate_summary
       - Integration with call_ai_api from Phase 15

    3. **UI behaviors**:
       - Loading spinner during API call
       - Buttons disabled during generation
       - Result display area with copy/regenerate buttons
       - Chinese error messages for failures
  </what-built>
  <how-to-verify>
    ### Prerequisites
    1. Ensure AI service is configured (visit /ai-config as admin)
    2. Ensure there are some records for the test user (create a few test records)
    3. Login as a regular user (not admin)

    ### Test Steps

    **Step 1: Verify UI Display**
    - Navigate to home page (/)
    - Verify AI Summary card appears between stats row and recent submissions
    - Verify time range dropdown has 4 options
    - Verify template dropdown has options (if templates exist)
    - Verify Generate button is visible

    **Step 2: Test Basic Summary Generation (SUMMARY-01)**
    - Select "本周" from time range dropdown
    - Click "生成总结" button
    - Verify loading spinner appears (UI-01)
    - Verify button is disabled during generation (UI-02)
    - Wait for response (up to 30 seconds)
    - Verify summary content appears in result area
    - Verify summary is formatted HTML (not raw text)

    **Step 3: Test Template Selection (SUMMARY-02)**
    - Select a template from dropdown (e.g., "周报模板")
    - Click "生成总结"
    - Verify generated summary reflects template style

    **Step 4: Test Custom Prompt (SUMMARY-03)**
    - Enter custom prompt: "简洁风格，突出重点工作"
    - Click "生成总结"
    - Verify generated summary reflects custom prompt influence

    **Step 5: Test Copy Functionality (SUMMARY-04)**
    - After successful generation, click "复制" button
    - Verify button text changes to "已复制"
    - Verify content is copied to clipboard (paste elsewhere to check)

    **Step 6: Test Regenerate (SUMMARY-04)**
    - Click "重新生成" button
    - Verify new summary is generated with same parameters

    **Step 7: Test Error Handling**
    - If AI config is invalid, verify Chinese error message appears
    - If network fails, verify "网络错误" message appears
  </how-to-verify>
  <resume-signal>Type "approved" to confirm feature works, or describe issues encountered</resume-signal>
</task>

</tasks>

<verification>
- User tested basic summary generation
- User tested template selection
- User tested custom prompt
- User tested copy functionality
- User tested regenerate
- User tested error handling
- All UI-01 and UI-02 behaviors verified
</verification>

<success_criteria>
- AI Summary card visible on home page
- Summary generation produces meaningful content
- Loading state and button disabling work correctly
- Copy button copies text to clipboard
- Error messages display in Chinese
- User confirms feature is functional
</success_criteria>

<output>
After completion, create `.planning/phases/17-personal-summary-generation/17-03-SUMMARY.md`
</output>