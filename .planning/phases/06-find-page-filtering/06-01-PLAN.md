---
phase: 06-find-page-filtering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [utils.py, tests/test_utils.py]
autonomous: true
requirements: [FIND-02]
user_setup: []

must_haves:
  truths:
    - "DateRange.TIME_RANGES contains 'last_7_days' key with value '最近 7 天'"
    - "DateRange.get_range('last_7_days') returns a 7-day date range"
  artifacts:
    - path: "utils.py"
      provides: "TIME_RANGES dict with last_7_days entry"
      contains: "'last_7_days': '最近 7 天'"
    - path: "tests/test_utils.py"
      provides: "Test for last_7_days in TIME_RANGES"
      contains: "test_last_7_days_in_time_ranges"
  key_links:
    - from: "utils.py TIME_RANGES"
      to: "DateRange.get_range()"
      via: "key lookup"
      pattern: "'last_7_days'.*lambda.*last_n_days"
---

<objective>
Add 'last_7_days' time range option to DateRange class for FIND-02 default date filter.

Purpose: Enable the template to default to "最近 7 天" in the date filter dropdown.
Output: Modified utils.py with new time range, passing test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-find-page-filtering/06-RESEARCH.md
@.planning/phases/06-find-page-filtering/06-VALIDATION.md
</context>

<interfaces>
<!-- Key interfaces from existing codebase -->

From utils.py (lines 7-13):
```python
TIME_RANGES = {
    'this_week': '本周',
    'last_week': '上周',
    'this_month': '本月',
    'this_quarter': '本季度',
    'this_year': '本年',
}
```

From utils.py (lines 76-92):
```python
@staticmethod
def get_range(time_range):
    time_range_methods = {
        'this_week': DateRange.this_week,
        'last_week': DateRange.last_week,
        'this_month': DateRange.this_month,
        'this_quarter': DateRange.this_quarter,
        'this_year': DateRange.this_year,
    }
    if time_range in time_range_methods:
        start_date, end_date = time_range_methods[time_range]()
    else:
        start_date, end_date = DateRange.this_year()
    return start_date, end_date
```

From utils.py (lines 34-39):
```python
@staticmethod
def last_n_days(n):
    today = DateRange.get_today()
    start_date = today - timedelta(days=n)
    end_date = today
    return start_date, end_date
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 'last_7_days' to DateRange class</name>
  <files>utils.py, tests/test_utils.py</files>
  <read_first>
    - /home/one/weekly/utils.py (current TIME_RANGES and get_range implementation)
    - /home/one/weekly/tests/test_utils.py (existing DateRange test patterns)
  </read_first>
  <behavior>
    - Test: test_last_7_days_in_time_ranges verifies 'last_7_days' key exists in TIME_RANGES
    - Test: DateRange.get_range('last_7_days') returns tuple of (start_date, end_date) where (end_date - start_date).days == 7
    - Test: TIME_RANGES['last_7_days'] == '最近 7 天'
  </behavior>
  <action>
    1. Add test to tests/test_utils.py:
       ```python
       def test_last_7_days_in_time_ranges(self):
           """Test that 'last_7_days' exists in TIME_RANGES with correct label."""
           assert 'last_7_days' in DateRange.TIME_RANGES
           assert DateRange.TIME_RANGES['last_7_days'] == '最近 7 天'

       def test_get_range_last_7_days(self):
           """Test that get_range('last_7_days') returns 7-day range."""
           from datetime import timedelta
           start, end = DateRange.get_range('last_7_days')
           assert (end - start).days == 7
       ```

    2. Modify utils.py TIME_RANGES dict (lines 7-13) to add 'last_7_days' as FIRST entry:
       ```python
       TIME_RANGES = {
           'last_7_days': '最近 7 天',  # Added for FIND-02 default
           'this_week': '本周',
           'last_week': '上周',
           'this_month': '本月',
           'this_quarter': '本季度',
           'this_year': '本年',
       }
       ```

    3. Modify utils.py get_range() method (lines 79-85) to add 'last_7_days' mapping:
       ```python
       time_range_methods = {
           'last_7_days': lambda: DateRange.last_n_days(7),  # Added for FIND-02
           'this_week': DateRange.this_week,
           'last_week': DateRange.last_week,
           'this_month': DateRange.this_month,
           'this_quarter': DateRange.this_quarter,
           'this_year': DateRange.this_year,
       }
       ```

    IMPORTANT: Add 'last_7_days' as the FIRST entry in both TIME_RANGES and time_range_methods dicts so it appears first in the dropdown menu.
  </action>
  <verify>
    <automated>pytest tests/test_utils.py::TestDateRange::test_last_7_days_in_time_ranges tests/test_utils.py::TestDateRange::test_get_range_last_7_days -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "'last_7_days': '最近 7 天'" utils.py
    - grep -q "'last_7_days': lambda" utils.py
    - pytest tests/test_utils.py::TestDateRange -x passes
  </acceptance_criteria>
  <done>
    - TIME_RANGES dict contains 'last_7_days': '最近 7 天' as first entry
    - get_range('last_7_days') returns a 7-day date range
    - All existing DateRange tests still pass
  </done>
</task>

</tasks>

<verification>
- Run: pytest tests/test_utils.py -x
- All DateRange tests pass including new last_7_days tests
</verification>

<success_criteria>
- DateRange.TIME_RANGES['last_7_days'] == '最近 7 天'
- DateRange.get_range('last_7_days') returns (start_date, end_date) where delta is 7 days
- No existing tests broken
</success_criteria>

<output>
After completion, create `.planning/phases/06-find-page-filtering/06-01-SUMMARY.md`
</output>