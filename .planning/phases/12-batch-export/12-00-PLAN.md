---
phase: 12-batch-export
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: [tests/test_exporters.py]
autonomous: true
requirements: [BATCH-01]
must_haves:
  truths:
    - "TestBatchExport class exists in test file"
    - "Tests define expected behavior for batch ZIP export"
  artifacts:
    - path: "tests/test_exporters.py"
      provides: "Test scaffolding for batch export"
      contains: "class TestBatchExport"
  key_links:
    - from: "tests/test_exporters.py"
      to: "routes.py"
      via: "batch_export route test"
      pattern: "def test_batch_export"
---

<objective>
Add test scaffolding for batch export functionality to define expected behavior before implementation.

Purpose: Establish test contracts that implementation must satisfy.
Output: TestBatchExport class with failing tests for ZIP generation and permission checks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-batch-export/12-RESEARCH.md
@.planning/phases/12-batch-export/12-VALIDATION.md

<interfaces>
From routes.py (existing patterns):
```python
# download_records pattern to follow
@app.route('/download_records', methods=['POST'])
@login_required
def download_records():
    query, start_date, end_date, _ = build_record_query(request.form)
    format = request.form.get('format', 'xlsx')
    exporter = ExporterFactory.get_exporter(format)
    output = exporter.export(records, title='周报')
    return send_file(output, mimetype=..., as_attachment=True, download_name=filename)
```

From models.py (permission pattern):
```python
@staticmethod
def managed_group(user):
    """Get groups managed by user."""
    groups = []
    all_permissions = User.all_permissions(user)
    if 'view_all' in all_permissions:
        groups = Group.query.options(joinedload(Group.users)).all()
    elif 'view_group' in all_permissions:
        # ... returns managed groups
    return groups
```

From exporters/__init__.py:
```python
exporter = ExporterFactory.get_exporter('pdf')  # or 'docx' or 'xlsx'
output = exporter.export(records, title='Report')
# output is BytesIO
# exporter.file_extension returns 'pdf', 'docx', or 'xlsx'
```

From tests/test_exporters.py (existing pattern):
- Each test class follows consistent structure
- Tests use MagicMock for Record objects
- Tests verify BytesIO output and file structure
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TestBatchExport class with test stubs</name>
  <files>tests/test_exporters.py</files>
  <read_first>
    - tests/test_exporters.py (see existing TestPdfExporter and TestDocxExporter patterns)
    - routes.py (download_records pattern for reference)
    - models.py (User.managed_group and permission patterns)
    - .planning/phases/12-batch-export/12-RESEARCH.md (ZIP creation pattern)
  </read_first>
  <action>
    Append TestBatchExport class to tests/test_exporters.py following the existing pattern.

    Add these test methods:

    1. `test_create_zip_with_files`: Create a ZIP in memory using zipfile.ZipFile + BytesIO, add files with names like "user1_20260320.pdf", verify ZIP contains expected files

    2. `test_batch_export_returns_bytesio`: Test batch_export helper function returns BytesIO with ZIP magic bytes 'PK'

    3. `test_batch_export_filename_format`: Verify filename format is `{username}_{date_range}.{ext}`

    4. `test_batch_export_groups_by_user`: Test that records are correctly grouped by username before export

    Each test should:
    - Import zipfile and BytesIO from stdlib
    - Create mock records with user relationships
    - Verify ZIP structure and contents

    Tests will FAIL until implementation is complete in Plan 01.
  </action>
  <verify>
    <automated>pytest tests/test_exporters.py::TestBatchExport -v --collect-only 2>/dev/null | grep -c "test_"</automated>
    Expected: 4 (4 test methods collected)
  </verify>
  <acceptance_criteria>
    - File tests/test_exporters.py contains "class TestBatchExport"
    - File contains test method "test_create_zip_with_files"
    - File contains test method "test_batch_export_returns_bytesio"
    - File contains test method "test_batch_export_filename_format"
    - File contains test method "test_batch_export_groups_by_user"
    - Command shows 4 tests collected
  </acceptance_criteria>
  <done>TestBatchExport class exists with 4 test methods. Tests fail (expected) until Plan 01 implementation.</done>
</task>

</tasks>

<verification>
- Test file contains TestBatchExport class
- All 4 test methods defined
- Tests fail with expected behavior until implementation
</verification>

<success_criteria>
- TestBatchExport class added to tests/test_exporters.py
- 4 test methods defined covering ZIP creation, BytesIO output, filename format, and grouping
- `pytest tests/test_exporters.py::TestBatchExport --collect-only` shows 4 tests
</success_criteria>

<output>
After completion, create `.planning/phases/12-batch-export/12-00-SUMMARY.md`
</output>