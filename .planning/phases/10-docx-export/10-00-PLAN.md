---
phase: 10-docx-export
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: [tests/test_exporters.py]
autonomous: true
requirements: [DOCX-01, DOCX-02]
must_haves:
  truths:
    - "Tests define expected DocxExporter behavior before implementation"
    - "Tests fail initially, guiding implementation"
  artifacts:
    - path: "tests/test_exporters.py"
      provides: "Test scaffolding for DocxExporter"
      contains: "class TestDocxExporter"
  key_links:
    - from: "tests/test_exporters.py::TestDocxExporter"
      to: "exporters/docx.py"
      via: "import and test"
      pattern: "from exporters.docx import DocxExporter"
---

<objective>
Create test scaffolding for DocxExporter class that defines expected behavior for DOCX export functionality.

Purpose: Establish test-first development approach; tests will FAIL initially and pass after Plan 01 implementation.
Output: Extended test file with TestDocxExporter class containing failing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@tests/test_exporters.py

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From exporters/base.py:
```python
class ExporterBase(ABC):
    def export(self, records: List[Any], **options) -> BytesIO: ...
    @abstractmethod
    def _generate(self, data: List[Any], options: Dict) -> BytesIO: ...
    @property
    @abstractmethod
    def file_extension(self) -> str: ...
    @property
    @abstractmethod
    def mime_type(self) -> str: ...
```

From exporters/image_resolver.py:
```python
class ImageResolver:
    def resolve_url(self, url: str) -> Optional[str]: ...
    def get_image_bytes(self, url: str) -> Optional[bytes]: ...
    def image_exists(self, url: str) -> bool: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TestDocxExporter class with failing tests</name>
  <files>tests/test_exporters.py</files>
  <behavior>
    - Test 1: file_extension property returns 'docx'
    - Test 2: mime_type property returns 'application/vnd.openxmlformats-officedocument.wordprocessingml.document'
    - Test 3: export() returns BytesIO with valid DOCX content (ZIP magic bytes: PK)
    - Test 4: _extract_images() helper extracts img tags from HTML
    - Test 5: _add_image_to_document() embeds image bytes into document
  </behavior>
  <action>
    Add a new test class `TestDocxExporter` to tests/test_exporters.py after the existing TestPdfExporter class.

    The test class should verify:
    1. `test_file_extension`: DocxExporter(uploads_path='/tmp').file_extension == 'docx'
    2. `test_mime_type`: DocxExporter(uploads_path='/tmp').mime_type == 'application/vnd.openxmlformats-officedocument.wordprocessingml.document'
    3. `test_export_returns_bytesio`: Mock record with content, verify BytesIO returned with DOCX magic bytes (b'PK' - DOCX is a ZIP file)
    4. `test_html_to_docx_conversion`: Verify HTML elements (p, strong, em, ul, ol, table, h1-h3, a, pre/code) are converted to DOCX equivalents
    5. `test_image_embedding`: Create temp image file, mock record with img src="/files/test.png", verify image is embedded in document

    Per research: htmldocx does NOT support images natively. Tests should expect:
    - _extract_images(html) method that returns list of (element, url) tuples
    - _add_image_to_document(document, url, image_bytes) method for embedding

    Follow the existing test pattern from TestPdfExporter - use MagicMock for records, tempfile for test images.
  </action>
  <verify>
    <automated>pytest tests/test_exporters.py::TestDocxExporter -v</automated>
  </verify>
  <done>
    TestDocxExporter class exists with 5 failing tests (tests fail because DocxExporter not implemented yet).
    Tests run without import errors (test file syntax is valid).
  </done>
</task>

</tasks>

<verification>
Run `pytest tests/test_exporters.py::TestDocxExporter -v` and confirm:
- All 5 tests are discovered and run
- Tests fail with ImportError or AttributeError (expected - implementation not yet created)
</verification>

<success_criteria>
- tests/test_exporters.py contains TestDocxExporter class
- 5 test methods exist with clear test names
- pytest discovers and runs the tests (they fail as expected)
</success_criteria>

<output>
After completion, create `.planning/phases/10-docx-export/10-00-SUMMARY.md`
</output>