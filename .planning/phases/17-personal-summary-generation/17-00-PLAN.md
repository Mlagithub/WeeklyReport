---
phase: 17-personal-summary-generation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: [tests/test_summary_generation.py]
autonomous: true
requirements: [SUMMARY-01, SUMMARY-02, SUMMARY-03, SUMMARY-04]
user_setup: []

must_haves:
  truths:
    - "Test file exists with scaffold tests defining expected behavior"
    - "All test methods document expected inputs and outputs"
  artifacts:
    - path: "tests/test_summary_generation.py"
      provides: "Test scaffolds for summary generation"
      min_lines: 50
  key_links: []
---

<objective>
Create test infrastructure for personal summary generation feature.

Purpose: Define expected behavior through test scaffolds before implementation (TDD pattern).
Output: Test file with documented test methods for all summary generation functions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key interfaces from prior phases -->

From ai_utils.py (Phase 15):
```python
def call_ai_api(prompt: str, user_id: int, function_type: str, timeout: int = 30) -> tuple[bool, str | None, str | None]:
    """Call OpenAI-compatible API for AI generation."""
    # Returns (success, processed_html_content, error_message)

def process_ai_response(content: str) -> str:
    """Process AI response: strip whitespace, convert Markdown to HTML."""
```

From models.py (Phase 16):
```python
class AITemplate(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    name = db.Column(db.String(100), unique=True, nullable=False)
    content = db.Column(db.Text, nullable=False)
    time_range = db.Column(db.String(20), nullable=False)
    created_at = db.Column(db.DateTime, default=datetime.utcnow)

    @staticmethod
    def get_by_time_range(time_range):
        """Get all templates for a specific time range."""
        return AITemplate.query.filter_by(time_range=time_range).all()

class Record(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    content = db.Column(db.Text)
    date = db.Column(db.Date(), index=True)
    createtime = db.Column(db.DateTime, default=datetime.utcnow)

class User(db.Model, FsUserMixin):
    records = db.relationship("Record", secondary="user_records", backref="user")
```

From utils.py:
```python
class DateRange:
    TIME_RANGES = {
        "this_week": "本周",
        "last_week": "上周",
        "this_month": "本月",
        "this_quarter": "本季度",
        "this_year": "本年",
    }

    @staticmethod
    def get_range(time_range):
        """Returns (start_date, end_date) for the given time_range key."""

def html_to_text(html_content):
    """Convert HTML to plain text for AI prompts."""
```

From utils/template_defaults.py (Phase 16):
```python
DEFAULT_TEMPLATES = [
    {
        "name": "周报模板",
        "time_range": "weekly",
        "content": "请根据以下记录生成{time_range}工作总结...\n用户：{user_name}\n记录数：{record_count}\n日期范围：{date_range}\n记录内容：\n{records}"
    },
    # ... monthly, quarterly, yearly templates
]

# Placeholders: {time_range}, {user_name}, {records}, {record_count}, {date_range}
```

From tests/conftest.py:
```python
@pytest.fixture
def client():
    """Create a test client with in-memory database."""

@pytest.fixture
def test_user(client):
    """Create a test user and return user data."""

@pytest.fixture
def auth_client(client, test_user):
    """Authenticated test client."""

@pytest.fixture
def admin_user(client):
    """Create an admin user for testing admin-only features."""

@pytest.fixture
def admin_client(client, admin_user):
    """Authenticated admin client."""
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test scaffold for summary generation functions</name>
  <files>tests/test_summary_generation.py</files>
  <behavior>
    - Test fetch_user_records: query user records for time range
    - Test assemble_prompt: fill template placeholders with data
    - Test generate_summary: orchestrate API call with assembled prompt
    - Test /generate-summary route: POST request handling
  </behavior>
  <action>
    Create tests/test_summary_generation.py with test scaffold classes:

    1. TestFetchUserRecords class (SUMMARY-01):
       - test_fetch_user_records_returns_records_in_range: pass with docstring
       - test_fetch_user_records_empty_range_returns_empty_list: pass with docstring
       - test_fetch_user_records_converts_html_to_text: pass with docstring

    2. TestAssemblePrompt class (SUMMARY-02, SUMMARY-03):
       - test_assemble_prompt_fills_all_placeholders: pass with docstring
       - test_assemble_prompt_with_custom_prompt: pass with docstring
       - test_assemble_prompt_without_template: pass with docstring
       - test_assemble_prompt_formats_records_list: pass with docstring

    3. TestGenerateSummary class (SUMMARY-01, SUMMARY-04):
       - test_generate_summary_calls_api: pass with docstring
       - test_generate_summary_returns_html: pass with docstring
       - test_generate_summary_handles_api_error: pass with docstring
       - test_generate_summary_handles_empty_records: pass with docstring

    4. TestGenerateSummaryRoute class (SUMMARY-01, UI-01, UI-02):
       - test_route_requires_login: pass with docstring
       - test_route_returns_json_response: pass with docstring
       - test_route_validates_time_range: pass with docstring

    Each test method should:
    - Use pass statement (stub test pattern)
    - Include detailed docstring documenting expected behavior
    - Specify inputs, expected outputs, and error handling
    - Reference requirement IDs (SUMMARY-01, etc.)
  </action>
  <verify>
    <automated>pytest tests/test_summary_generation.py --collect-only -q 2>&1 | grep -c "test_" | grep -E "^[1-9][0-9]*"</automated>
  </verify>
  <done>Test file exists with at least 10 scaffold test methods defining expected behavior for summary generation</done>
</task>

</tasks>

<verification>
- Test file created in tests/ directory
- All test classes organized by function group
- Each test method documents expected behavior with requirement reference
- Tests follow existing project test patterns (stub tests with pass + docstring)
</verification>

<success_criteria>
- tests/test_summary_generation.py exists with scaffold tests
- At least 10 test methods defined across 4 test classes
- All test methods have docstrings documenting expected behavior
</success_criteria>

<output>
After completion, create `.planning/phases/17-personal-summary-generation/17-00-SUMMARY.md`
</output>