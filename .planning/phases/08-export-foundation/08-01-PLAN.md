---
phase: 08-export-foundation
plan: 01
type: execute
wave: 1
depends_on: [08-00]
files_modified: [requirements.txt]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "python-docx package is installed and importable"
    - "weasyprint package is installed and importable"
    - "htmldocx package is installed and importable"
  artifacts:
    - path: "requirements.txt"
      provides: "Project dependencies"
      contains: "python-docx"
      contains: "weasyprint"
      contains: "htmldocx"
  key_links: []
---

<objective>
Update requirements.txt with export dependencies for PDF, DOCX, and HTML-to-DOCX conversion.

Purpose: Install the core libraries needed for multi-format document export. These are verified versions from research.
Output: Updated requirements.txt with python-docx, WeasyPrint, and htmldocx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-export-foundation/08-RESEARCH.md
</context>

<interfaces>
<!-- Current requirements.txt contents (read before modification) -->

Current dependencies in requirements.txt:
```
argon2_cffi==23.1.0
beautifulsoup4==4.12.3
bleach==6.2.0
Bootstrap_Flask==2.4.1
Faker==30.8.2
Flask==3.0.3
flask_admin==2.0.0a2
Flask_CKEditor==1.0.0
flask_security==5.5.2
flask_sqlalchemy==3.1.1
flask_wtf==1.2.2
openpyxl==3.1.5
python_dateutil==2.9.0.post0
WTForms==3.2.1
gunicorn==25.1.0
pytest==8.3.5
pytest-cov==5.0.0
```

Already installed (supporting dependencies):
- beautifulsoup4==4.12.3 (HTML parsing)
- openpyxl==3.1.5 (Excel)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add export dependencies to requirements.txt</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt (current dependencies)
  </read_first>
  <action>
Add the following three lines to requirements.txt (maintain alphabetical order where possible):

```
htmldocx==0.0.6
python-docx==1.2.0
weasyprint==68.1
```

Insert placement:
- Add `htmldocx==0.0.6` after `gunicorn==25.1.0` (alphabetically before openpyxl)
- Add `python-docx==1.2.0` after `pytest-cov==5.0.0` (alphabetically before python_dateutil)
- Add `weasyprint==68.1` after `WTForms==3.2.1` (end of file)

Note: These specific versions are verified from PyPI:
- python-docx 1.2.0 (June 2025 release, industry standard for DOCX)
- WeasyPrint 68.1 (active maintenance, pure Python PDF generation)
- htmldocx 0.0.6 (HTML-to-DOCX bridge, unmaintained but functional)

Do NOT modify existing dependencies. Only add the three new lines.
  </action>
  <verify>
    <automated>grep "python-docx==1.2.0" requirements.txt && grep "weasyprint==68.1" requirements.txt && grep "htmldocx==0.0.6" requirements.txt</automated>
  </verify>
  <acceptance_criteria>
    - File requirements.txt contains line "htmldocx==0.0.6"
    - File requirements.txt contains line "python-docx==1.2.0"
    - File requirements.txt contains line "weasyprint==68.1"
    - All existing dependencies remain unchanged
  </acceptance_criteria>
  <done>All three export libraries added to requirements.txt with exact versions specified.</done>
</task>

<task type="auto">
  <name>Task 2: Install dependencies and verify</name>
  <files></files>
  <read_first>
    - requirements.txt (updated dependencies)
  </read_first>
  <action>
Run pip install to install the new dependencies:

```bash
pip install -r requirements.txt
```

Then verify each package is importable by running:
```bash
python -c "import docx; print(f'python-docx: {docx.__version__}')"
python -c "import weasyprint; print(f'weasyprint: {weasyprint.__version__}')"
python -c "import htmldocx; print('htmldocx: installed')"
```

If any import fails, the task is not complete. Investigate and resolve import errors.
  </action>
  <verify>
    <automated>python -c "import docx; assert docx.__version__ == '1.2.0'" && python -c "import weasyprint; assert weasyprint.__version__ == '68.1'" && python -c "import htmldocx"</automated>
  </verify>
  <acceptance_criteria>
    - Command `python -c "import docx"` succeeds without error
    - Command `python -c "import weasyprint"` succeeds without error
    - Command `python -c "import htmldocx"` succeeds without error
    - python-docx version is 1.2.0
    - WeasyPrint version is 68.1
  </acceptance_criteria>
  <done>All three export libraries installed and importable in Python environment.</done>
</task>

</tasks>

<verification>
- requirements.txt updated with three new dependencies
- All packages installed and importable
- TestDependencies tests in tests/test_exporters.py pass after this plan
</verification>

<success_criteria>
- requirements.txt contains python-docx==1.2.0, weasyprint==68.1, htmldocx==0.0.6
- All packages install without errors
- All packages importable in Python
</success_criteria>

<output>
After completion, create `.planning/phases/08-export-foundation/08-01-SUMMARY.md`
</output>