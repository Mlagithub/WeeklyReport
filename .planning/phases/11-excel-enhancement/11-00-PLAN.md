---
phase: 11-excel-enhancement
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: [tests/test_exporters.py]
autonomous: true
requirements: [XLSX-01]
must_haves:
  truths:
    - "TestExcelExporter class exists in test file"
    - "Tests define expected behavior for rich text conversion"
  artifacts:
    - path: "tests/test_exporters.py"
      provides: "Test scaffolding for ExcelExporter"
      contains: "class TestExcelExporter"
  key_links:
    - from: "tests/test_exporters.py"
      to: "exporters/excel.py"
      via: "import statement"
      pattern: "from exporters.excel import ExcelExporter"
---

<objective>
Add test scaffolding for ExcelExporter to define expected behavior before implementation.

Purpose: Establish test contracts that implementation must satisfy.
Output: TestExcelExporter class with failing tests for rich text conversion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-excel-enhancement/11-RESEARCH.md
@.planning/phases/11-excel-enhancement/11-VALIDATION.md

<interfaces>
From exporters/base.py:
```python
class ExporterBase(ABC):
    def export(self, records: List[Any], **options) -> BytesIO
    @abstractmethod
    def _generate(self, data: List[Any], options: Dict) -> BytesIO
    @property
    @abstractmethod
    def file_extension(self) -> str
    @property
    @abstractmethod
    def mime_type(self) -> str
```

From tests/test_exporters.py (existing pattern):
- TestPdfExporter class pattern to follow
- TestDocxExporter class pattern to follow
- Each test class has: test_file_extension, test_mime_type, test_export_returns_bytesio
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TestExcelExporter class with test stubs</name>
  <files>tests/test_exporters.py</files>
  <read_first>
    - tests/test_exporters.py (see existing TestPdfExporter and TestDocxExporter patterns)
    - exporters/base.py (understand ExporterBase interface)
    - .planning/phases/11-excel-enhancement/11-RESEARCH.md (reference for HTML-to-CellRichText behavior)
  </read_first>
  <action>
    Append TestExcelExporter class to tests/test_exporters.py following the existing pattern from TestPdfExporter and TestDocxExporter.

    Add these test methods:

    1. `test_file_extension`: Verify ExcelExporter.file_extension returns 'xlsx'

    2. `test_mime_type`: Verify ExcelExporter.mime_type returns 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'

    3. `test_export_returns_bytesio`: Create mock records, call export(), verify BytesIO with XLSX magic bytes (ZIP header 'PK')

    4. `test_html_to_rich_text_bold`: Test _html_to_rich_text method with `<strong>Bold text</strong>` returns CellRichText with bold InlineFont

    5. `test_html_to_rich_text_italic`: Test with `<em>Italic text</em>` returns CellRichText with italic InlineFont

    6. `test_html_to_rich_text_underline`: Test with `<u>Underline text</u>` returns CellRichText with u='single' (NOT boolean True)

    7. `test_html_to_rich_text_nested`: Test with `<strong>Bold <em>and italic</em></strong>` handles nested formatting

    8. `test_rich_text_in_cell`: After export, verify cell value is CellRichText instance

    Each test should import ExcelExporter from exporters.excel. Tests will FAIL until implementation is complete in Plan 01.
  </action>
  <verify>
    <automated>pytest tests/test_exporters.py::TestExcelExporter -v --collect-only 2>/dev/null | grep -c "test_"</automated>
    Expected: 8 (8 test methods collected)
  </verify>
  <acceptance_criteria>
    - File tests/test_exporters.py contains "class TestExcelExporter"
    - File contains test method "test_file_extension"
    - File contains test method "test_mime_type"
    - File contains test method "test_export_returns_bytesio"
    - File contains test method "test_html_to_rich_text_bold"
    - File contains test method "test_html_to_rich_text_italic"
    - File contains test method "test_html_to_rich_text_underline"
    - File contains test method "test_html_to_rich_text_nested"
    - File contains test method "test_rich_text_in_cell"
    - Command `pytest tests/test_exporters.py::TestExcelExporter --collect-only` shows 8 tests collected
  </acceptance_criteria>
  <done>TestExcelExporter class exists with 8 test methods. Tests fail (expected) until Plan 01 implementation.</done>
</task>

</tasks>

<verification>
- Test file contains TestExcelExporter class
- All 8 test methods defined
- Tests fail with ImportError until Plan 01 creates exporters/excel.py
</verification>

<success_criteria>
- TestExcelExporter class added to tests/test_exporters.py
- 8 test methods defined covering file_extension, mime_type, export, and rich text conversion
- `pytest tests/test_exporters.py::TestExcelExporter --collect-only` shows 8 tests
</success_criteria>

<output>
After completion, create `.planning/phases/11-excel-enhancement/11-00-SUMMARY.md`
</output>