---
phase: 19-code-review-redundancy-style
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: true
requirements: [CODE-REVIEW-01]
user_setup: []

must_haves:
  truths:
    - "All linting issues are identified and documented"
    - "Critical bugs (undefined names) are flagged"
    - "Unused imports are identified"
    - "Import sorting issues are catalogued"
  artifacts:
    - path: ".planning/phases/19-code-review-redundancy-style/19-LINTING-REPORT.md"
      provides: "Complete catalog of all ruff linting issues"
      exports: ["issues_by_file", "severity_counts"]
  key_links:
    - from: "ruff check"
      to: "Python source files"
      via: ".venv/bin/ruff check ."
      pattern: "ruff check"
---

<objective>
Run ruff to identify all linting issues in Python source files.

Purpose: Establish a complete baseline of code quality issues before fixes.
Output: LINTING-REPORT.md with categorized issues.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-code-review-redundancy-style/19-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run ruff linting analysis</name>
  <files>.planning/phases/19-code-review-redundancy-style/19-LINTING-REPORT.md</files>
  <read_first>
    pyproject.toml (ruff configuration)
    ai_utils.py (check for defined functions)
    routes.py (check imports section)
    summary_utils.py (check imports section)
  </read_first>
  <action>
    Run ruff check using the virtual environment's ruff binary:

    ```bash
    /home/one/projects/WeeklyReport/.venv/bin/ruff check . --output-format=json > /tmp/ruff-issues.json
    ```

    Parse the JSON output and create a categorized report in 19-LINTING-REPORT.md with:

    1. **Summary Statistics:**
       - Total issues found
       - Issues by severity (error/warning)
       - Issues by code (F821, F401, I001, W292)

    2. **Critical Issues (F821 - Undefined names):**
       - File: routes.py
       - Line: 483
       - Issue: `decrypt_api_key` used but not imported
       - Fix: Add import from ai_utils

    3. **Unused Imports (F401):**
       - routes.py:22 - SummaryGenerationForm imported but unused
       - summary_utils.py:14 - Group imported but unused

    4. **Import Sorting Issues (I001):**
       - List all 10 files with unsorted imports

    5. **Missing Newlines (W292):**
       - List all 8 files missing trailing newline

    Format each entry as:
    ```markdown
    | File | Line | Code | Issue | Fix |
    |------|------|------|-------|-----|
    | routes.py | 483 | F821 | Undefined `decrypt_api_key` | Add import |
    ```
  </action>
  <verify>
    <automated>.planning/phases/19-code-review-redundancy-style/19-LINTING-REPORT.md exists and contains issue count >= 21</automated>
  </verify>
  <done>LINTING-REPORT.md exists with categorized issues, total count matches ruff output (21 issues)</done>
</task>

</tasks>

<verification>
- LINTING-REPORT.md exists
- Report contains all 21 issues identified by ruff
- Critical F821 bug is prominently flagged
- Unused imports (F401) are documented
- Import sorting issues (I001) are catalogued
- Missing newlines (W292) are listed
</verification>

<success_criteria>
- Complete catalog of all linting issues
- Issues categorized by severity and type
- Clear fix recommendations for each issue
- Ready for Wave 1 to apply fixes
</success_criteria>

<output>
After completion, create `.planning/phases/19-code-review-redundancy-style/19-00-SUMMARY.md`
</output>