---
phase: 08-export-foundation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: [tests/test_exporters.py]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Test file exists for exporters module"
    - "Test stubs define expected behavior for ExporterBase, ExporterFactory, ImageResolver"
  artifacts:
    - path: "tests/test_exporters.py"
      provides: "Test scaffolding for export infrastructure"
      min_lines: 50
      contains: "class TestExporterBase"
  key_links: []
---

<objective>
Create test scaffolding for the exporters module before implementation.

Purpose: Define test stubs that specify expected behavior for ExporterBase, ExporterFactory, and ImageResolver. These tests will guide implementation in Wave 1.
Output: tests/test_exporters.py with failing tests that define expected contracts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-export-foundation/08-RESEARCH.md
@.planning/phases/08-export-foundation/08-VALIDATION.md
</context>

<interfaces>
<!-- Key patterns from existing test structure -->

From tests/conftest.py:
```python
@pytest.fixture
def client():
    """Create a test client with in-memory database."""
    app.config['TESTING'] = True
    # ... fixture pattern

@pytest.fixture
def test_user(client):
    """Create a test user and return user data."""
```

From pytest.ini:
```ini
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create test_exporters.py with test stubs</name>
  <files>tests/test_exporters.py</files>
  <read_first>
    - tests/conftest.py (fixture patterns)
    - pytest.ini (test configuration)
  </read_first>
  <action>
Create tests/test_exporters.py with the following test class stubs. Each test class should have test methods that FAIL initially (using pytest.fail or assert False) to define expected behavior:

1. **TestExporterBase** class with tests:
   - `test_export_method_exists`: Verify ExporterBase has `export(records, **options)` method
   - `test_file_extension_property`: Verify `file_extension` property exists and is abstract
   - `test_mime_type_property`: Verify `mime_type` property exists and is abstract
   - `test_cannot_instantiate_base`: Verify ExporterBase cannot be instantiated directly

2. **TestExporterFactory** class with tests:
   - `test_register_exporter`: Verify `register(format, exporter_class)` class method
   - `test_get_exporter_returns_instance`: Verify `get_exporter(format)` returns exporter instance
   - `test_get_exporter_invalid_format_raises`: Verify ValueError for unsupported format
   - `test_supported_formats_returns_list`: Verify `supported_formats()` returns list of strings

3. **TestImageResolver** class with tests:
   - `test_resolve_url_files_prefix`: Verify `/files/filename.jpg` converts to absolute path
   - `test_resolve_url_external_returns_none`: Verify external URLs return None
   - `test_get_image_bytes_returns_bytes`: Verify `get_image_bytes(url)` returns bytes or None
   - `test_image_exists_returns_bool`: Verify `image_exists(url)` returns boolean

4. **TestDependencies** class with tests:
   - `test_python_docx_installed`: Verify `python-docx` is importable
   - `test_weasyprint_installed`: Verify `weasyprint` is importable
   - `test_htmldocx_installed`: Verify `htmldocx` is importable

Import structure at top:
```python
import pytest
from io import BytesIO
from typing import List, Any
```

Do NOT create actual exporter classes in this task - only test stubs that define expected behavior.
  </action>
  <verify>
    <automated>grep -c "class TestExporterBase" tests/test_exporters.py && grep -c "class TestExporterFactory" tests/test_exporters.py && grep -c "class TestImageResolver" tests/test_exporters.py && grep -c "class TestDependencies" tests/test_exporters.py</automated>
  </verify>
  <acceptance_criteria>
    - File tests/test_exporters.py exists with at least 50 lines
    - Contains "class TestExporterBase:" with at least 4 test methods
    - Contains "class TestExporterFactory:" with at least 4 test methods
    - Contains "class TestImageResolver:" with at least 4 test methods
    - Contains "class TestDependencies:" with at least 3 test methods
    - Each test method starts with "test_" prefix
    - All tests currently fail (stubs define behavior, not implementation)
  </acceptance_criteria>
  <done>Test file exists with stubs for ExporterBase, ExporterFactory, ImageResolver, and Dependencies. Running pytest tests/test_exporters.py shows failing tests (expected - implementation comes in Wave 1).</done>
</task>

</tasks>

<verification>
- Test file created: `tests/test_exporters.py`
- All 4 test classes present
- Test methods follow pytest naming conventions
</verification>

<success_criteria>
- tests/test_exporters.py exists with test class stubs
- File contains TestExporterBase, TestExporterFactory, TestImageResolver, TestDependencies classes
- Each class has multiple test method stubs
</success_criteria>

<output>
After completion, create `.planning/phases/08-export-foundation/08-00-SUMMARY.md`
</output>