---
phase: 08-export-foundation
plan: 02
type: execute
wave: 1
depends_on: [08-00]
files_modified: [exporters/base.py, exporters/__init__.py]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "ExporterBase class exists and cannot be instantiated directly"
    - "ExporterBase defines abstract interface with export(), file_extension, mime_type"
    - "Subclasses must implement _generate(), file_extension, mime_type"
  artifacts:
    - path: "exporters/base.py"
      provides: "Abstract base class for all exporters"
      min_lines: 40
      exports: ["ExporterBase"]
      contains: "class ExporterBase"
      contains: "@abstractmethod"
  key_links: []
---

<objective>
Create the ExporterBase abstract base class that defines the common interface for all format-specific exporters.

Purpose: Establish the template method pattern for export operations. All future exporters (PDF, DOCX, Excel) will inherit from this base class.
Output: exporters/base.py with ExporterBase abstract class.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-export-foundation/08-RESEARCH.md
@tests/test_exporters.py
</context>

<interfaces>
<!-- Pattern from RESEARCH.md - ExporterBase implementation -->

From 08-RESEARCH.md (Pattern 1: Abstract Base Class):
```python
from abc import ABC, abstractmethod
from io import BytesIO
from typing import List, Dict, Any

class ExporterBase(ABC):
    """Abstract base class for document exporters.

    Implements template method pattern for common export flow.
    """

    def export(self, records: List[Any], **options) -> BytesIO:
        """Template method defining export flow."""
        data = self._prepare_data(records, options)
        output = self._generate(data, options)
        return output

    @abstractmethod
    def _generate(self, data: List[Any], options: Dict) -> BytesIO:
        """Format-specific generation logic. Must be implemented by subclasses."""
        pass

    def _prepare_data(self, records: List[Any], options: Dict) -> List[Any]:
        """Common data preparation (override if needed)."""
        return records

    @property
    @abstractmethod
    def file_extension(self) -> str:
        """Return file extension without dot (e.g., 'pdf', 'docx')."""
        pass

    @property
    @abstractmethod
    def mime_type(self) -> str:
        """Return MIME type for send_file()."""
        pass
```

From tests/test_exporters.py (expected test behaviors):
- TestExporterBase.test_export_method_exists
- TestExporterBase.test_file_extension_property
- TestExporterBase.test_mime_type_property
- TestExporterBase.test_cannot_instantiate_base
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create exporters directory</name>
  <files>exporters/__init__.py</files>
  <read_first>
    - utils.py (existing pattern for module structure)
    - config.py (configuration patterns)
  </read_first>
  <action>
Create the `exporters/` directory and an empty `__init__.py` file:

```bash
mkdir -p /home/one/weekly/exporters
touch /home/one/weekly/exporters/__init__.py
```

The __init__.py should initially be empty or contain only:
```python
# Exporters module - format-specific document exporters
# Phase 8: Infrastructure setup
```

Note: ExporterFactory will be added to __init__.py in Plan 03.
  </action>
  <verify>
    <automated>test -d /home/one/weekly/exporters && test -f /home/one/weekly/exporters/__init__.py</automated>
  </verify>
  <acceptance_criteria>
    - Directory /home/one/weekly/exporters exists
    - File /home/one/weekly/exporters/__init__.py exists
  </acceptance_criteria>
  <done>exporters/ directory created with empty __init__.py.</done>
</task>

<task type="auto">
  <name>Task 2: Create ExporterBase abstract class</name>
  <files>exporters/base.py</files>
  <read_first>
    - tests/test_exporters.py (expected behavior from test stubs)
    - utils.py (existing patterns - RecordDownloader class structure)
  </read_first>
  <action>
Create exporters/base.py with the ExporterBase abstract class. Implement exactly as specified in RESEARCH.md Pattern 1:

```python
"""Abstract base class for document exporters.

This module defines the template method pattern for export operations.
All format-specific exporters (PDF, DOCX, Excel) inherit from ExporterBase.
"""

from abc import ABC, abstractmethod
from io import BytesIO
from typing import List, Dict, Any


class ExporterBase(ABC):
    """Abstract base class for document exporters.

    Implements template method pattern for common export flow.
    Subclasses must implement _generate(), file_extension, and mime_type.
    """

    def export(self, records: List[Any], **options) -> BytesIO:
        """Template method defining export flow.

        Args:
            records: List of Record objects to export
            **options: Format-specific options (filename, title, etc.)

        Returns:
            BytesIO buffer containing the generated document
        """
        data = self._prepare_data(records, options)
        output = self._generate(data, options)
        return output

    @abstractmethod
    def _generate(self, data: List[Any], options: Dict) -> BytesIO:
        """Format-specific generation logic.

        Must be implemented by subclasses.

        Args:
            data: Prepared data (from _prepare_data)
            options: Format-specific options

        Returns:
            BytesIO buffer containing the generated document
        """
        pass

    def _prepare_data(self, records: List[Any], options: Dict) -> List[Any]:
        """Common data preparation hook.

        Override in subclasses for format-specific preprocessing.
        Default implementation returns records unchanged.

        Args:
            records: Raw Record objects
            options: Export options

        Returns:
            Prepared data for _generate()
        """
        return records

    @property
    @abstractmethod
    def file_extension(self) -> str:
        """Return file extension without dot.

        Returns:
            File extension (e.g., 'pdf', 'docx', 'xlsx')
        """
        pass

    @property
    @abstractmethod
    def mime_type(self) -> str:
        """Return MIME type for send_file().

        Returns:
            MIME type string (e.g., 'application/pdf')
        """
        pass
```

Key implementation notes:
- `export()` is the template method - calls _prepare_data then _generate
- `_generate()` is abstract - subclasses MUST implement
- `_prepare_data()` has default implementation - subclasses CAN override
- `file_extension` and `mime_type` are abstract properties
  </action>
  <verify>
    <automated>python -c "from exporters.base import ExporterBase; print('ExporterBase imported successfully')"</automated>
  </verify>
  <acceptance_criteria>
    - File exporters/base.py exists with at least 40 lines
    - Contains "class ExporterBase(ABC):"
    - Contains "@abstractmethod" decorator
    - Contains "def export(self, records: List[Any], **options) -> BytesIO:"
    - Contains "def _generate(self, data: List[Any], options: Dict) -> BytesIO:"
    - Contains "def _prepare_data(self, records: List[Any], options: Dict) -> List[Any]:"
    - Contains "def file_extension(self) -> str:" with @property and @abstractmethod
    - Contains "def mime_type(self) -> str:" with @property and @abstractmethod
    - Cannot instantiate ExporterBase directly (raises TypeError)
  </acceptance_criteria>
  <done>ExporterBase abstract class created with template method pattern. Class cannot be instantiated directly.</done>
</task>

<task type="auto">
  <name>Task 3: Run tests to verify ExporterBase</name>
  <files></files>
  <read_first>
    - tests/test_exporters.py (test expectations)
    - exporters/base.py (implementation)
  </read_first>
  <action>
Run the TestExporterBase tests to verify the implementation:

```bash
pytest tests/test_exporters.py::TestExporterBase -v
```

All tests should now pass:
- test_export_method_exists: PASS
- test_file_extension_property: PASS
- test_mime_type_property: PASS
- test_cannot_instantiate_base: PASS

If any test fails, fix the implementation in exporters/base.py.
  </action>
  <verify>
    <automated>pytest tests/test_exporters.py::TestExporterBase -v --tb=short 2>&1 | grep -E "(PASSED|FAILED|ERROR)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - Command `pytest tests/test_exporters.py::TestExporterBase -v` exits with code 0
    - All 4 TestExporterBase tests show PASSED
  </acceptance_criteria>
  <done>All TestExporterBase tests pass. ExporterBase implementation verified.</done>
</task>

</tasks>

<verification>
- exporters/base.py exists with ExporterBase class
- ExporterBase is abstract (ABC) with template method pattern
- All TestExporterBase tests pass
</verification>

<success_criteria>
- exporters/base.py contains ExporterBase abstract class
- Class has export(), _generate(), _prepare_data() methods
- Class has file_extension and mime_type abstract properties
- Cannot instantiate ExporterBase directly
- All TestExporterBase tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-export-foundation/08-02-SUMMARY.md`
</output>