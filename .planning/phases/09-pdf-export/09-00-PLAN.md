---
phase: 09-pdf-export
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: [tests/test_exporters.py]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "TestPdfExporter class exists in test_exporters.py"
    - "Test methods define expected PdfExporter behavior"
  artifacts:
    - path: "tests/test_exporters.py"
      provides: "Test scaffolding for PdfExporter"
      contains: "class TestPdfExporter"
  key_links:
    - from: "tests/test_exporters.py"
      to: "exporters/pdf.py"
      via: "import PdfExporter"
      pattern: "from exporters.pdf import PdfExporter"
---

<objective>
Add test scaffolding for PdfExporter class to existing test_exporters.py file.

Purpose: Define expected behavior for PdfExporter before implementation (TDD approach).
Output: Test stubs that will pass once PdfExporter is implemented in Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-pdf-export/09-RESEARCH.md
@.planning/phases/09-pdf-export/09-VALIDATION.md

<interfaces>
<!-- Key interfaces from Phase 8 that PdfExporter must implement -->

From exporters/base.py:
```python
class ExporterBase(ABC):
    def export(self, records: List[Any], **options) -> BytesIO:
        """Template method - calls _prepare_data() then _generate()"""

    @abstractmethod
    def _generate(self, data: List[Any], options: Dict) -> BytesIO:
        """Format-specific generation - MUST implement"""

    @property
    @abstractmethod
    def file_extension(self) -> str:
        """Return 'pdf'"""

    @property
    @abstractmethod
    def mime_type(self) -> str:
        """Return 'application/pdf'"""
```

From exporters/image_resolver.py:
```python
class ImageResolver:
    def __init__(self, uploads_path: str)
    def resolve_url(self, url: str) -> Optional[str]
    def get_image_bytes(self, url: str) -> Optional[bytes]
```

From exporters/__init__.py:
```python
class ExporterFactory:
    @classmethod
    def register(cls, format: str, exporter_class: Type[ExporterBase]) -> None
    @classmethod
    def get_exporter(cls, format: str) -> ExporterBase
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TestPdfExporter test stubs</name>
  <files>tests/test_exporters.py</files>
  <action>
Add a new `TestPdfExporter` class to tests/test_exporters.py with the following test methods:

1. `test_file_extension` - Verify PdfExporter.file_extension returns 'pdf'
2. `test_mime_type` - Verify PdfExporter.mime_type returns 'application/pdf'
3. `test_export_returns_bytesio` - Verify export() returns BytesIO with PDF content
4. `test_image_embedding` - Verify url_fetcher resolves /files/ URLs for image embedding
5. `test_headers_footers` - Verify CSS Paged Media generates headers with title and footers with page numbers

Use pytest.fail() for stub tests that require implementation. Mark tests that need actual PDF content verification with appropriate assertions.

Reference the existing TestExporterBase and TestImageResolver classes for style consistency.
  </action>
  <verify>
    <automated>pytest tests/test_exporters.py::TestPdfExporter -v</automated>
  </verify>
  <done>TestPdfExporter class exists with 5 test methods, tests fail with "not implemented" messages (expected for Wave 0)</done>
</task>

</tasks>

<verification>
- [ ] TestPdfExporter class added to tests/test_exporters.py
- [ ] All 5 test methods defined with pytest.fail() stubs
- [ ] Tests fail as expected (implementation comes in Plan 01)
</verification>

<success_criteria>
Test scaffolding complete - tests define expected PdfExporter behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/09-pdf-export/09-00-SUMMARY.md`
</output>