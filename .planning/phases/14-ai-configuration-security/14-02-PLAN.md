---
phase: 14-ai-configuration-security
plan: 02
type: execute
wave: 1
depends_on: [14-00]
files_modified: [forms.py]
autonomous: true
requirements: [CONFIG-01]
user_setup: []

must_haves:
  truths:
    - "Form validates API URL must start with http:// or https://"
    - "Form requires API Key field (not empty)"
    - "Form requires Model Name field (not empty)"
    - "Form provides placeholders for user guidance"
    - "Form has Test Connection and Save Configuration buttons"
  artifacts:
    - path: "forms.py"
      provides: "AIConfigForm for admin UI"
      contains: "class AIConfigForm"
      pattern: "api_url.*StringField.*Regexp"
  key_links:
    - from: "forms.py"
      to: "WTForms validators"
      via: "DataRequired, Regexp"
      pattern: "from wtforms.validators import"
---

<objective>
Create AIConfigForm with validation for AI service configuration fields.

Purpose: CONFIG-01 requires form for API URL/Key/Model input with proper validation.
Output: AIConfigForm in forms.py with URL validation, required fields, and submit buttons.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-ai-configuration-security/14-CONTEXT.md
@.planning/phases/14-ai-configuration-security/14-RESEARCH.md
@.planning/phases/14-ai-configuration-security/14-UI-SPEC.md
@.planning/phases/14-ai-configuration-security/14-00-SUMMARY.md

<interfaces>
From forms.py (existing patterns):
```python
from flask_wtf import FlaskForm
from wtforms import StringField, PasswordField, SubmitField
from wtforms.validators import DataRequired, EqualTo, Length

class MyRegisterForm(FlaskForm):
    username = StringField("用户名", validators=[DataRequired(), Length(min=2, max=255)])
    password = PasswordField("密码", validators=[DataRequired(), Length(min=8, max=18)])
    submit = SubmitField("注册")

class ThemeForm(FlaskForm):
    theme_name = SelectField("", choices=choices, default="lumen")
    submit = SubmitField("更改主题")
```

From UI-SPEC.md (form field specifications):
| Field | Type | Class | Validation |
|-------|------|-------|------------|
| api_url | StringField | form-control | Required, URL format (https?://) |
| api_key | PasswordField | form-control | Required, masked display |
| model_name | StringField | form-control | Required |
| test_submit | SubmitField | btn btn-outline-secondary | Test Connection |
| submit | SubmitField | btn btn-primary | Save Configuration |

Copywriting (UI-SPEC.md):
- Field labels: API URL / API Key / 模型名称
- Field placeholders: https://api.openai.com/v1 / (password field, no placeholder) / gpt-4o-mini
- Validation errors: API URL不能为空 / API URL格式无效，必须以http://或https://开头 / API Key不能为空 / 模型名称不能为空
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 2-1: Create AIConfigForm class</name>
  <files>forms.py</files>
  <read_first>
    - /home/one/projects/WeeklyReport/forms.py
    - /home/one/projects/WeeklyReport/templates/config.html
  </read_first>
  <action>
    Add AIConfigForm to forms.py following existing WTForms patterns.

    Per UI-SPEC.md: Form fields with specific labels, placeholders, validation messages.
    Per RESEARCH.md Pattern 2: Regexp validator for URL format.
    Per CONTEXT.md D-locked: Chinese validation messages.

    Add after ThemeForm class (around line 135):

    ```python
    class AIConfigForm(FlaskForm):
        """Form for AI service configuration.

        Per CONFIG-01: AI service configuration storage.
        Per UI-SPEC.md: Field labels, placeholders, validation messages in Chinese.
        """

        api_url = StringField(
            "API URL",
            validators=[
                DataRequired(message="API URL不能为空"),
                Regexp(r'^https?://.+', message="API URL格式无效，必须以http://或https://开头")
            ],
            render_kw={"placeholder": "https://api.openai.com/v1"}
        )
        api_key = PasswordField(
            "API Key",
            validators=[DataRequired(message="API Key不能为空")],
            description="保存后仅显示最后4位字符"
        )
        model_name = StringField(
            "模型名称",
            validators=[DataRequired(message="模型名称不能为空")],
            render_kw={"placeholder": "gpt-4o-mini"}
        )
        test_submit = SubmitField("测试连接", render_kw={"class": "btn btn-outline-secondary mb-3"})
        submit = SubmitField("保存配置", render_kw={"class": "btn btn-primary"})
    ```

    Import Regexp validator at top of forms.py (add to existing imports):
    ```python
    from wtforms.validators import DataRequired, EqualTo, Length, Regexp
    ```

    Pattern matches existing forms.py structure with Chinese validation messages.
  </action>
  <verify>
    <automated>grep -q "class AIConfigForm" /home/one/projects/WeeklyReport/forms.py && grep -q "Regexp.*https://" /home/one/projects/WeeklyReport/forms.py && grep -q "test_submit.*SubmitField.*测试连接" /home/one/projects/WeeklyReport/forms.py && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - forms.py contains "class AIConfigForm(FlaskForm):"
    - Regexp imported in wtforms.validators line
    - api_url field: StringField with DataRequired and Regexp validator
    - api_key field: PasswordField with DataRequired validator
    - model_name field: StringField with DataRequired validator
    - test_submit: SubmitField("测试连接") with btn-outline-secondary class
    - submit: SubmitField("保存配置") with btn-primary class
    - api_url has placeholder "https://api.openai.com/v1"
    - model_name has placeholder "gpt-4o-mini"
    - api_key has description "保存后仅显示最后4位字符"
    - Validation messages in Chinese as specified
  </acceptance_criteria>
  <done>AIConfigForm added to forms.py with URL validation and submit buttons</done>
</task>

</tasks>

<verification>
Run pytest to verify form class exists:
```bash
pytest tests/test_ai_config.py::TestAIConfigForm::test_form_exists -v --tb=short
```
</verification>

<success_criteria>
- AIConfigForm exists in forms.py
- URL validation with Regexp for https?:// pattern
- All fields required with Chinese error messages
- Test and Save buttons with correct CSS classes
</success_criteria>

<output>
After completion, create `.planning/phases/14-ai-configuration-security/14-02-SUMMARY.md`
</output>