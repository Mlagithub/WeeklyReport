---
quick_id: 260328-gi7
description: Remove batch export from Phase 12, keeping download functionality
---

# Quick Task: Remove Batch Export

## Objective
Remove batch export functionality added in Phase 12 while preserving the regular download functionality.

## Files to Modify

| File | Action | Lines |
|------|--------|-------|
| routes.py | Remove batch_export route | 349-405 |
| templates/manage_records.html | Remove batch export form + JS | 156-168, 186-189 |
| tests/test_exporters.py | Remove TestBatchExport class | 646-759 |
| exporters/batch.py | Delete file | entire file |

## Tasks

### Task 1: Remove batch_export route from routes.py
Remove the entire batch_export route function (lines 349-405), including the decorator and function body.

### Task 2: Remove batch export button from manage_records.html
Remove:
- The `{% if not hide_groups %}` block containing the batch export form (lines 156-168)
- The JavaScript that populates batchExportForm time_range (lines 186-189)

Keep the download form intact.

### Task 3: Remove TestBatchExport class from tests
Remove the entire TestBatchExport class (lines 646-759) from the test file.

### Task 4: Delete exporters/batch.py
Delete the entire batch.py file from exporters module.

## Verification
- `grep -c "batch_export" routes.py` should return 0
- `grep -c "batch_export" templates/manage_records.html` should return 0
- `grep -c "TestBatchExport" tests/test_exporters.py` should return 0
- `test -f exporters/batch.py` should fail (file deleted)
- `pytest tests/ -v -x` should pass