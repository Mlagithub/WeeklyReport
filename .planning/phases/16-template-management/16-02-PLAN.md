---
phase: 16-template-management
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified: [routes.py, templates/admin/ai_templates.html, utils/template_defaults.py]
autonomous: true
requirements: [TEMPLATE-01, TEMPLATE-03]
user_setup: []
must_haves:
  truths:
    - "Admin can create, edit, and delete templates via dedicated page"
    - "Four default templates exist after initialization"
    - "Template list shows all templates with edit/delete buttons"
    - "Template form validates and saves correctly"
  artifacts:
    - path: "templates/admin/ai_templates.html"
      provides: "Template management UI"
      contains: "template CRUD form"
    - path: "utils/template_defaults.py"
      provides: "Default template initialization"
      exports: ["initialize_default_templates", "DEFAULT_TEMPLATES"]
    - path: "routes.py"
      provides: "Template CRUD routes"
      contains: "def ai_templates"
  key_links:
    - from: "routes.py"
      to: "utils/template_defaults.py"
      via: "initialize_default_templates() on first access"
      pattern: "initialize_default_templates"
    - from: "templates/admin/ai_templates.html"
      to: "/ai-templates"
      via: "form submission"
      pattern: "url_for.*ai_templates"
---

<objective>
Create admin CRUD route, UI, and default template initialization.

Purpose: Complete template management system per TEMPLATE-01, initialize defaults per TEMPLATE-03.
Output: Route handler, HTML template, default template initialization utility.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-template-management/16-01-SUMMARY.md

<interfaces>
<!-- Existing patterns from routes.py and templates -->

From routes.py ai_config pattern:
```python
@app.route("/ai-config")
@login_required
@with_db_transaction
def ai_config():
    """AI configuration route - admin only."""
    if not current_user.is_admin:
        flash("需要管理员权限", "warning")
        return redirect(url_for("home"))
    # ... form handling
```

From templates/config.html pattern:
```html
<div class="card">
  <div class="card-header">
    <h5>{{ render_icon('cog') }} AI 配置</h5>
  </div>
  <div class="card-body">
    <form method="POST">
      {{ form.hidden_tag() }}
      <!-- fields -->
    </form>
  </div>
</div>
```

Required default templates per TEMPLATE-03:
1. weekly: "本周周报总结" template with {time_range}, {user_name}, {records}
2. monthly: "本月月报总结" template
3. quarterly: "本季度总结" template
4. yearly: "本年度总结" template
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create template defaults utility</name>
  <files>utils/template_defaults.py</files>
  <action>Create new file utils/template_defaults.py with:
1. DEFAULT_TEMPLATES list of 4 template dicts:
   - {"name": "周报模板", "time_range": "weekly", "content": "请根据以下记录生成{time_range}工作总结...\n用户：{user_name}\n记录数：{record_count}\n日期范围：{date_range}\n记录内容：\n{records}"}
   - {"name": "月报模板", "time_range": "monthly", "content": "..."}
   - {"name": "季度报模板", "time_range": "quarterly", "content": "..."}
   - {"name": "年报模板", "time_range": "yearly", "content": "..."}
2. initialize_default_templates() function:
   - Check if AITemplate.query.count() == 0
   - If empty, create templates from DEFAULT_TEMPLATES
   - db.session.add_all() + db.session.commit()
   - Return True if initialized, False if already exists

Import AITemplate from models, db from extensions.</action>
  <verify>
    <automated>grep -q "DEFAULT_TEMPLATES" utils/template_defaults.py && grep -q "initialize_default_templates" utils/template_defaults.py && grep -q "周报模板" utils/template_defaults.py && grep -q "weekly" utils/template_defaults.py && echo "PASS"</automated>
  </verify>
  <done>utils/template_defaults.py exists with DEFAULT_TEMPLATES and initialize_default_templates function</done>
</task>

<task type="auto">
  <name>Task 2: Add template CRUD route</name>
  <files>routes.py</files>
  <action>Add ai_templates route after ai_config route in register_routes function:
1. @app.route("/ai-templates", methods=["GET", "POST"])
2. @login_required + @with_db_transaction decorators
3. Admin check: if not current_user.is_admin -> flash + redirect
4. Call initialize_default_templates() on GET to ensure defaults exist (per TEMPLATE-03)
5. GET: render template list with TemplateForm for create/edit
6. POST with form.validate_on_submit():
   - Create new AITemplate from form data
   - OR edit existing if template_id in form data
   - flash success message + redirect
7. Handle delete via separate route /ai-templates/delete/<id>

Include edit and delete routes as separate functions following existing patterns.</action>
  <verify>
    <automated>grep -q "def ai_templates" routes.py && grep -q "initialize_default_templates" routes.py && grep -q "/ai-templates/delete" routes.py && grep -q "/ai-templates/edit" routes.py && echo "PASS"</automated>
  </verify>
  <done>Template CRUD routes exist: list, create, edit, delete with admin permission check</done>
</task>

<task type="auto">
  <name>Task 3: Create admin template management UI</name>
  <files>templates/admin/ai_templates.html</files>
  <action>Create templates/admin/ai_templates.html extending base.html:
1. Title: "AI 模板管理"
2. Card layout with template list table:
   - Columns: 名称, 时间范围, 操作
   - Each row has Edit and Delete buttons
3. Create/Edit form card:
   - TemplateForm rendered with Bootstrap styling
   - Name, content (TextArea with placeholder hints), time_range dropdown
   - Save button
4. Content field placeholder hint showing available variables:
   "{time_range} {user_name} {records} {record_count} {date_range}"
5. Flash messages display for success/error

Follow config.html and manage_records.html patterns for styling.</action>
  <verify>
    <automated>test -f templates/admin/ai_templates.html && grep -q "AI 模板管理" templates/admin/ai_templates.html && grep -q "TemplateForm" templates/admin/ai_templates.html && grep -q "time_range" templates/admin/ai_templates.html && echo "PASS"</automated>
  </verify>
  <done>Admin template management page exists with list and form</done>
</task>

</tasks>

<verification>
- Template CRUD routes work (create, edit, delete)
- Default templates initialized on first access
- Admin UI displays templates and form
- Placeholder hint visible in content field
</verification>

<success_criteria>
Template management system complete. Admin can manage templates. Default templates initialized.
</success_criteria>

<output>
After completion, create `.planning/phases/16-template-management/16-02-SUMMARY.md`
</output>